{% include 'components/header/announcement-bar.twig' %}

{# ═══ Header — Noor: nav left, logo center, actions right ═══ #}
{% set is_theme_preview = theme is defined and theme.mode is defined and theme.mode == 'preview' %}
{% set header_transparent = theme.settings.get('header_transparent', true) %}
{% set logo_light = theme.settings.get('logo_light', store.logo) %}
{% set logo_dark  = theme.settings.get('logo_dark', store.logo) %}
{% set logo_h     = theme.settings.get('logo_height', '36') %}

<header class="nr-header{% if header_transparent and (is_page('index') or is_theme_preview) %} nr-header--transparent{% endif %}" id="nr-header">
  <div class="nr-header__inner">

    {# Left: menu toggle + nav #}
    <div class="nr-header__left">
      <button class="nr-header__icon nr-header__menu-toggle" aria-label="{{ trans('blocks.header.main_menu') }}">
        <svg viewBox="0 0 24 24"><path d="M3 12h18M3 6h18M3 18h18"/></svg>
      </button>
      <div class="nr-header__nav">
        <custom-main-menu></custom-main-menu>
      </div>
    </div>

    {# Center: logo #}
    <a href="{{ store.url }}" class="nr-header__logo">
      {% if logo_light %}
        <img class="nr-header__logo-light" src="{{ logo_light }}" alt="{{ store.name }}" style="height:{{ logo_h }}px;">
      {% endif %}
      {% if logo_dark %}
        <img class="nr-header__logo-dark" src="{{ logo_dark }}" alt="{{ store.name }}" style="height:{{ logo_h }}px;">
      {% endif %}
      {% if not logo_light and not logo_dark %}
        <span class="nr-header__logo-text">{{ store.name }}</span>
      {% endif %}
    </a>
    {% if is_page('index') or is_theme_preview %}
    <h1 class="sr-only">{{ store.name }}</h1>
    {% endif %}

    {# Right: actions #}
    <div class="nr-header__actions">

      {# Language / Currency Switcher #}
      {% if store.settings.is_multilingual or store.settings.currencies_enabled %}
      <button type="button" class="nr-header__locale-btn" onclick="salla.event.dispatch('localization::open')">
        <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"/><path d="M2 12h20"/><path d="M12 2a15.3 15.3 0 0 1 4 10 15.3 15.3 0 0 1-4 10 15.3 15.3 0 0 1-4-10 15.3 15.3 0 0 1 4-10z"/></svg>
        <span class="nr-header__locale-label">{{ user.language.code|upper }}</span>
      </button>
      {% endif %}

      {# Search #}
      <button type="button" class="nr-header__icon" onclick="salla.event.dispatch('search::open')" aria-label="{{ trans('blocks.header.search') }}">
        <svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
      </button>

      {# Wishlist #}
      <a href="{{ link('/wishlist') }}" class="nr-header__icon" aria-label="{{ trans('blocks.header.wishlist') }}">
        <svg viewBox="0 0 24 24"><path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/></svg>
      </a>

      {# Account #}
      {% if user.type == 'user' %}
        <salla-user-menu avatar-only show-header></salla-user-menu>
      {% else %}
        <button type="button" class="nr-header__icon" onclick="salla.event.dispatch('login::open')" aria-label="{{ trans('blocks.header.account') }}">
          <svg viewBox="0 0 24 24"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
        </button>
      {% endif %}

      {# Cart — use slot="icon" like Raed so Salla renders its count badge #}
      <salla-cart-summary>
        <svg slot="icon" class="nr-header__cart-icon" viewBox="0 0 24 24"><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg>
      </salla-cart-summary>

    </div>
  </div>
</header>
